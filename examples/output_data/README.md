# Example outputs

Notebooks write generated files here — CG trajectories, PLUMED PDBs, water reference structures, and similar outputs. Inputs live in `input_data/`.

This folder is gitignored (except this README), so local outputs are not committed.
