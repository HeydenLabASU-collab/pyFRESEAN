# FRESEAN COARSE (C) reference data

Stored FRESEAN COARSE outputs for ``c_ref`` pytest checks
(pyfresean vs original C implementation).

| Directory | System |
|-----------|--------|
| `ala_dipeptide_gas_300K/` | Alanine dipeptide, gas, 300 K (CG + FRESEAN) |

See each subdirectory's README for regenerate and test commands.
